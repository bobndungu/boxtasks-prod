langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_goal_workspace
    - node.type.goal
    - node.type.workspace
id: node.goal.field_goal_workspace
field_name: field_goal_workspace
entity_type: node
bundle: goal
label: Workspace
description: 'The workspace this goal belongs to.'
required: true
translatable: true
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:node'
  handler_settings:
    target_bundles:
      workspace: workspace
    sort:
      field: title
      direction: ASC
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
